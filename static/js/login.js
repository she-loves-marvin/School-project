const form = document.querySelector('form')
form.addEventListener('sign In', function(){

    const formData= new FormData();
    const email= document.getElementById("email");
    const Password =document.getElementById("[password");
})